LIGHTRAG_HOST="lightrag"
LIGHTRAG_PORT=9621
# Provide at runtime via --env-file, not baked into image.
LIGHTRAG_API_KEY="your-LightRAG-api-key-here"
MCP_TRANSPORT="streamable-http"
MCP_HTTP_HOST="0.0.0.0"
MCP_HTTP_PORT=8000
MCP_HTTP_PATH="/"
MCP_HTTP_STATELESS=false
MCP_HTTP_JSON_RESPONSE=false
